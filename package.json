{
  "name": "fsrs-web-app",
  "version": "0.1.0",
  "private": true,
  "description": "Free Spaced Repetition Scheduler — web app",
  "scripts": {
    "serve": "npx serve . -p 3000",
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}
